# The 08bb:2902 adapter advertises sample-rate control but rejects SET_CUR.
# Reuse the kernel's Optoplay quirk, which skips that broken control transfer.
options snd_usb_audio quirk_alias=08bb2902:0a920053
